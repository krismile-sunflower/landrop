{
  "name": "landrop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix worker",
    "build": "npm run build --prefix client && npm run typecheck --prefix worker",
    "dev": "npm run build --prefix client && npm run dev --prefix worker",
    "dev:client": "npm run dev --prefix client",
    "deploy": "npm run build --prefix client && npm run deploy --prefix worker",
    "typecheck": "npm run typecheck --prefix client && npm run typecheck --prefix worker",
    "lint": "npm run lint --prefix client && npm run lint --prefix worker"
  }
}
