{
  "name": "landrop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server && npm install --prefix worker",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "build:cloudflare": "npm run build --prefix client && npm run typecheck --prefix worker",
    "deploy:cloudflare": "npm run build --prefix client && npm run deploy --prefix worker",
    "start": "npm run start --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:cloudflare": "npm run build --prefix client && npm run dev --prefix worker",
    "typecheck": "npm run typecheck --prefix client && npm run typecheck --prefix server && npm run typecheck --prefix worker",
    "lint": "npm run lint --prefix client && npm run lint --prefix server && npm run lint --prefix worker"
  }
}
